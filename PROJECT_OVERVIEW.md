# 项目概览

## 📋 项目信息

**项目名称**: BSC流动性池监控系统  
**技术栈**: Next.js + PostgreSQL + ethers.js  
**语言**: JavaScript (ES6+)  
**区块链**: BSC (Binance Smart Chain)  
**DEX**: PancakeSwap V2

## 🎯 核心功能

### 1. 实时监控
- 监听PancakeSwap V2 Factory合约的`PairCreated`事件
- 自动为新创建的交易对启动监听器
- 追踪Swap、Mint、Burn、Sync事件

### 2. 数据收集
- 交易记录（买入/卖出）
- 流动性变化（添加/移除）
- 代币信息（名称、符号、精度）
- 储备量和价格信息

### 3. 数据分析
- 最近1小时交易统计
- 大额交易识别
- 流动性净变化计算
- 小时级别数据聚合

### 4. 智能警报
- 大额买入/卖出警告
- 流动性激增提醒
- Rug Pull风险检测
- 异常交易量警告

### 5. Web界面
- 实时仪表板
- 交易对列表和详情
- 大额交易展示
- 警报中心

## 📁 项目结构

```
lp-monitor/
├── src/
│   ├── blockchain/              # 区块链交互层
│   │   ├── provider.js          # RPC连接配置
│   │   └── tokenService.js      # 代币信息获取
│   │
│   ├── components/              # React组件
│   │   ├── Layout.js            # 页面布局（导航栏+页脚）
│   │   ├── PairCard.js          # 交易对卡片组件
│   │   └── StatCard.js          # 统计卡片组件
│   │
│   ├── contracts/               # 智能合约
│   │   └── abis.js              # Factory、Pair、ERC20 ABI定义
│   │
│   ├── db/                      # 数据库层
│   │   ├── client.js            # PostgreSQL连接池
│   │   ├── schema.sql           # 数据库表结构定义
│   │   └── repositories/        # 数据访问对象
│   │       ├── tokenRepository.js       # 代币CRUD
│   │       ├── pairRepository.js        # 交易对CRUD
│   │       ├── transactionRepository.js # 交易CRUD
│   │       ├── liquidityRepository.js   # 流动性事件CRUD
│   │       ├── alertRepository.js       # 警报CRUD
│   │       └── analyticsRepository.js   # 分析数据CRUD
│   │
│   ├── monitor/                 # 监控服务
│   │   ├── index.js             # 主启动脚本
│   │   ├── analyzer.js          # 数据分析引擎
│   │   └── listeners/           # 事件监听器
│   │       ├── factoryListener.js  # Factory事件监听
│   │       └── pairListener.js     # Pair事件监听
│   │
│   ├── pages/                   # Next.js页面
│   │   ├── api/                 # API路由
│   │   │   ├── pairs/           # 交易对API
│   │   │   │   ├── index.js     # 获取交易对列表
│   │   │   │   └── [address].js # 获取交易对详情
│   │   │   ├── transactions/    # 交易API
│   │   │   │   └── index.js     # 获取交易列表
│   │   │   ├── liquidity/       # 流动性API
│   │   │   │   └── index.js     # 获取流动性事件
│   │   │   ├── alerts/          # 警报API
│   │   │   │   └── index.js     # 获取/标记警报
│   │   │   ├── analytics/       # 分析API
│   │   │   │   └── top-pairs.js # 获取热门交易对
│   │   │   └── stats.js         # 获取总体统计
│   │   │
│   │   ├── pairs/               # 交易对页面
│   │   │   ├── index.js         # 交易对列表页
│   │   │   └── [address].js     # 交易对详情页
│   │   ├── _app.js              # Next.js应用入口
│   │   ├── _document.js         # HTML文档模板
│   │   ├── index.js             # 首页/仪表板
│   │   ├── transactions.js      # 大额交易页面
│   │   └── alerts.js            # 警报中心页面
│   │
│   ├── styles/                  # 样式文件
│   │   └── globals.css          # 全局CSS（Tailwind）
│   │
│   └── utils/                   # 工具函数
│       └── format.js            # 格式化函数（地址、时间、金额等）
│
├── .env.example                 # 环境变量示例
├── .eslintrc.json               # ESLint配置
├── .gitignore                   # Git忽略文件
├── jsconfig.json                # JavaScript配置
├── next.config.js               # Next.js配置
├── package.json                 # 项目依赖
├── postcss.config.js            # PostCSS配置
├── tailwind.config.js           # Tailwind CSS配置
├── start.sh                     # 启动脚本
├── README.md                    # 项目说明
├── INSTALL.md                   # 安装指南
└── PROJECT_OVERVIEW.md          # 本文件
```

## 🗄️ 数据库设计

### 表结构

1. **tokens** - 代币信息
   - address, name, symbol, decimals, total_supply
   - 索引: address, symbol

2. **pairs** - 交易对
   - address, token0_address, token1_address, reserve0, reserve1
   - 关联: token0_id → tokens, token1_id → tokens
   - 索引: address, token0_address, token1_address, created_at

3. **transactions** - 交易记录
   - pair_address, transaction_hash, amount0_in/out, amount1_in/out
   - 标记: is_large (大额交易)
   - 索引: pair_address, timestamp, is_large

4. **liquidity_events** - 流动性事件
   - pair_address, event_type (mint/burn), amount0, amount1
   - 索引: pair_address, event_type, timestamp

5. **sync_events** - 同步事件
   - pair_address, reserve0, reserve1
   - 用于追踪价格变化

6. **analytics** - 分析数据
   - hour_timestamp, transaction_count, volume, liquidity变化
   - 聚合: 按小时统计
   - 唯一索引: (pair_address, hour_timestamp)

7. **alerts** - 警报
   - alert_type, severity, title, description
   - 标记: is_read
   - 索引: pair_address, alert_type, severity, timestamp

8. **monitor_state** - 监控状态
   - 记录最后同步的区块号等状态信息

## 🔄 工作流程

### 监控流程

```
1. 启动 Factory 监听器
   ↓
2. 监听 PairCreated 事件
   ↓
3. 获取代币信息 (Token0, Token1)
   ↓
4. 保存交易对到数据库
   ↓
5. 为该交易对启动 Pair 监听器
   ↓
6. 监听 Swap, Mint, Burn, Sync 事件
   ↓
7. 保存事件到数据库
   ↓
8. 触发数据分析
   ↓
9. 生成警报（如果满足条件）
```

### 定时任务

- **每10分钟**: 更新所有交易对的小时级分析数据
- **每1小时**: 全面分析所有交易对，生成统计报告
- **每30秒**: 打印系统状态（活跃监听器数量等）

## 🔌 API接口

### 交易对相关
- `GET /api/pairs` - 获取交易对列表（支持分页）
- `GET /api/pairs/[address]` - 获取交易对详情

### 交易相关
- `GET /api/transactions` - 获取交易列表
- 参数: pairAddress (可选), limit

### 流动性相关
- `GET /api/liquidity` - 获取流动性事件
- 参数: pairAddress (可选), limit, hours

### 警报相关
- `GET /api/alerts` - 获取警报列表
- `PATCH /api/alerts` - 标记警报为已读
- 参数: unreadOnly, limit, offset

### 分析相关
- `GET /api/analytics/top-pairs` - 获取热门交易对
- `GET /api/stats` - 获取总体统计数据

## 🎨 前端页面

### 1. 仪表板 (/)
- 总体统计卡片（交易对数、交易数、流动性事件、未读警报）
- 最近1小时活动概览
- 最新创建的6个交易对

### 2. 交易对列表 (/pairs)
- 网格展示所有交易对
- 分页功能
- 显示代币信息、创建时间
- 点击查看详情

### 3. 交易对详情 (/pairs/[address])
- 交易对基本信息
- 储备量显示
- 最近1小时统计
- 最近20笔交易
- 最近20个流动性事件
- 链接到BSCScan

### 4. 大额交易 (/transactions)
- 表格展示最近24小时大额交易
- 显示时间、类型、金额、交易哈希等
- 实时更新（15秒刷新）

### 5. 警报中心 (/alerts)
- 所有警报列表
- 过滤：全部/未读
- 按严重程度标记颜色
- 一键标记已读
- 跳转到相关交易对

## 🚀 启动方式

### 方式1: 分别启动

终端1 - 监控服务:
```bash
npm run monitor
```

终端2 - Web服务:
```bash
npm run dev
```

### 方式2: 使用启动脚本

```bash
./start.sh
```
选择模式3可同时启动监控和Web服务。

### 方式3: 生产环境（PM2）

```bash
npm run build
pm2 start ecosystem.config.js
```

## 📊 监控指标

### 系统级别
- 活跃监听器数量
- 内存使用情况
- 最后同步区块号

### 业务级别
- 总交易对数
- 24小时交易数
- 24小时流动性事件数
- 未读警报数
- 最近1小时活跃交易对数

### 交易对级别
- 最近1小时交易数
- 最近1小时大额交易数
- 交易量（Token0/Token1）
- 流动性净变化
- 当前储备量
- 当前价格

## 🔐 安全考虑

1. **RPC密钥保护** - 不要将RPC URL提交到公共仓库
2. **数据库安全** - 使用强密码，限制访问权限
3. **输入验证** - API端点进行参数验证
4. **SQL注入防护** - 使用参数化查询
5. **速率限制** - 控制RPC请求频率

## ⚡ 性能优化

### 已实现
- PostgreSQL连接池
- 批量数据插入
- 索引优化
- 重复事件过滤（ON CONFLICT）

### 可优化
- 添加Redis缓存热点数据
- 实现请求队列限流
- 使用WebSocket推送实时数据
- 数据库分区（按时间）

## 🐛 调试技巧

### 查看日志
```bash
# 监控服务日志
tail -f monitor.log

# Next.js日志
# 直接在启动的终端查看
```

### 数据库查询
```bash
# 连接数据库
psql -d lp_monitor -U lp_user

# 查看最新交易对
SELECT * FROM pairs ORDER BY created_at DESC LIMIT 10;

# 查看交易统计
SELECT COUNT(*) FROM transactions;

# 查看未读警报
SELECT * FROM alerts WHERE is_read = false;
```

### 测试RPC连接
```javascript
const { testConnection } = require('./src/blockchain/provider');
testConnection();
```

## 📈 扩展建议

### 功能扩展
- [ ] 支持更多DEX（PancakeSwap V3、Biswap）
- [ ] 实现价格Oracle集成
- [ ] 添加K线图和交易量图表
- [ ] Telegram/Discord机器人通知
- [ ] 代币安全性评分
- [ ] 钱包追踪功能
- [ ] 历史数据回测

### 技术优化
- [ ] TypeScript重构（如需要）
- [ ] 添加单元测试
- [ ] Docker容器化部署
- [ ] CI/CD自动化
- [ ] 负载均衡
- [ ] 数据库主从复制

## 🤝 贡献指南

1. Fork 项目
2. 创建功能分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

## 📞 支持

- 📧 Email: 通过GitHub Issue联系
- 📖 文档: README.md, INSTALL.md
- 🐛 Bug报告: GitHub Issues
- 💡 功能建议: GitHub Discussions

## 📝 更新日志

### v1.0.0 (2024-11-24)
- ✨ 初始版本发布
- ✅ 实现Factory和Pair监听
- ✅ 完成数据库设计和实现
- ✅ 创建Web界面
- ✅ 实现基础分析和警报功能

---

**开发者**: AI Assistant  
**最后更新**: 2024-11-24  
**项目状态**: ✅ 可用

